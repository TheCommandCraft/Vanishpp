version: '3.8'

services:
  builder:
    image: maven:3.9-eclipse-temurin-21
    working_dir: /app
    volumes:
      - .:/app
      - maven-repo:/root/.m2
    command: mvn clean test

volumes:
  maven-repo:
